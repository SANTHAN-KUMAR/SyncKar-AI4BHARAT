# Railway deployment configuration for SyncKar
# https://docs.railway.app/reference/config-as-code
#
# This file controls the MAIN API service (SyncKar-AI4BHARAT).
#
# For the mock-systems service, add this variable in Railway dashboard
# under mock-systems → Variables:
#   RUN_MOCK_SYSTEMS = true
#
# Services (Railway free plan — 4 max):
#   1. SyncKar-AI4BHARAT  — main API + embedded Celery worker + beat
#   2. mock-systems        — all three mock dept systems on one service
#   3. Postgres            — managed plugin
#   4. Redis               — managed plugin

[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "bash -c 'if [ \"$RUN_MOCK_SYSTEMS\" = \"true\" ]; then uvicorn mock_systems.combined_app:app --host 0.0.0.0 --port $PORT; else alembic upgrade head && uvicorn synckar.api.main:app --host 0.0.0.0 --port $PORT; fi'"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
